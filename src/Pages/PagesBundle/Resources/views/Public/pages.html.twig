{% extends ":layout:Public/layout.html.twig" %}
{%  block body %}
<div class="container">
    <div class="row">

        <div class="span12">
            <h4> {{ page.title }}</h4>
            <p>
                {{ page.content }}
            </p>
        </div>
    </div>
</div>
{% endblock %}