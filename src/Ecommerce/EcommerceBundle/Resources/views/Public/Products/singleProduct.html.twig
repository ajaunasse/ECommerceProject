{% extends ":layout:layout.html.twig" %}
{% block body %}
    <div class="container">
        <div class="row">
            {% include ":modules:navigation.html.twig" %}
            <div class="span9">
                <div class="row">
                    <div class="span4">
                        <img src="{{ product.image }}" alt="img" width="470" height="310">
                    </div>
                    <div class="span5">
                        <h4>{{ product.category }}</h4>
                        <h5>{{ product.name }}</h5>
                        <p> {{ product.description }}</p>
                        <h4>{{ product.price }}€</h4>
                        <form action="panier.php">
                            <select name="qte" class="span1">
                                <option>1</option>
                            </select>

                            <div>
                                <button class="btn btn-primary">Ajouter au panier</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}